<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="utf-8" />
    <title>Initial Page</title>
    <style>
      /* 모달 스타일 */
      .modal {
        display: none;
        position: fixed;
        z-index: 1;
        left: 0;
        top: 0;
        width: 100%;
        height: 100%;
        background-color: rgba(0, 0, 0, 0.5);
      }
      .modal-content {
        background-color: white;
        margin: 15% auto;
        padding: 20px;
        border: 1px solid #888;
        width: 300px;
        text-align: center;
      }
      .modal button {
        margin: 5px;
      }
    </style>
  </head>
  <body>
    <h1>Web App Practice</h1>

    <% if (state === 'on') { %>
    <p>환영합니다, <%= userID %> 님!</p>
    <form action="/logout" method="POST" style="display: inline">
      <button type="submit">로그아웃</button>
    </form>
    <!-- 회원 탈퇴 버튼을 누르면 모달을 보여줍니다 -->
    <button type="button" onclick="showModal()">회원 탈퇴</button>
    <button onclick="window.location.href='/myInfo'">내 정보 보러 가기</button>
    <button onclick="window.location.href='/stocks'">
      주식 종목 보러 가기
    </button>
    <% } else { %>
    <button onclick="window.location.href='/login'">로그인</button>
    <button onclick="window.location.href='/signup'">회원가입</button>
    <button onclick="window.location.href='/stocks'">
      주식 종목 보러 가기
    </button>
    <% } %>

    <!-- 탈퇴 확인 모달 -->
    <div id="confirmModal" class="modal">
      <div class="modal-content">
        <p>정말 탈퇴하실 건가요?</p>
        <form action="/idDel" method="POST" style="display: inline">
          <button type="submit">예</button>
        </form>
        <button type="button" onclick="closeModal()">아니오</button>
      </div>
    </div>

    <script>
      // 모달 열기
      function showModal() {
        document.getElementById("confirmModal").style.display = "block";
      }

      // 모달 닫기
      function closeModal() {
        document.getElementById("confirmModal").style.display = "none";
      }
    </script>
  </body>
</html>
